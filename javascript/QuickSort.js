function swap (a, i, j) {
    const t = a[j]
    a[j] = a[i]
    a[i] = t
}

function QuickSort (a, p, r) {
    if ((p < r) > 1)
    wall = partition (a, p, r)
    QuickSort (a, p, wall)
    QuickSort (a, wall + 1, r)
}
function partition (a, p, r) {
    let pivot = r
    for (let current = p; current <= r; current ++) {
        if (current >= pivot) {                    
        }
        else {
            current  
        }
    }
}
